import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 1.5, 1.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-17 h-18 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>what is BitsNBytes.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        Welcome to the official website of BitsNbytes, 
        your gateway to the exciting world of technology and innovation! 
        We are a dynamic technical club operating under the esteemed Information Science Department, 
        dedicated to fostering a vibrant community of tech enthusiasts, learners, and creators.
      </motion.p>

      <motion.div variants={textVariant()}>
        
        <h2 className={styles.sectionHeadText}>What we Do</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        BitsNbytes is more than just a club; 
        it's a hub for innovation, learning, and creativity. 
        Our aim is to connect like-minded individuals who are 
        passionate about technology and are eager to explore 
        and excel in the ever-evolving tech landscape. 
      </motion.p><br/>

      <br /><motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Core Members Of</p>
        <h2 className={styles.sectionHeadText}>BitsNBytes.</h2>
      </motion.div>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");